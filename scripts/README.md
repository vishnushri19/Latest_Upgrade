# Scripts

This folder contains runnable entry points for the framework.

## Prerequisites

- Python 3.9+
- Network access to your BIG-IP (lab recommended)

## Setup

From the repository root:

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
pip install -e .
```
